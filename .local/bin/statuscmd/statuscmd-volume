#!/bin/sh

case $BUTTON in
  1) ;;
  2) st -e "$EDITOR" "$0" ;;
  3) pamixer -t; kill -10 $(pidof slstatus) ;;
  4) pamixer --allow-boost -i 1; kill -10 $(pidof slstatus) ;;
  5) pamixer --allow-boost -d 1; kill -10 $(pidof slstatus) ;;
esac
