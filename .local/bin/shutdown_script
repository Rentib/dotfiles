#!/bin/sh

choice=$(printf "shutdown\nsuspend\nreboot" | dmenu)
case $choice in
  "shutdown") prompt "Do you want to shutdown?" "shutdown -h now";;
  "suspend") systemctl suspend;;
  "reboot") prompt "Do you want to reboot?" "reboot";;
  *);;
esac
